spring:
  liquibase:
    enabled: false
  jpa:
    generate-ddl=false:
    hibernate:
      ddl-auto=create-drop:h2:
    console:
      enabled: true
      path: /h2
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/SICOSEL2_PRS1?currentSchema=rsl
    username:  postgres
    password:  qwe
    driverClassName: org.postgresql.Driver
  h2:
    console:
      enabled: true
      path: /console
      settings:
        trace: false
        web-allow-others: false
clientes:
  autenticacion: http://10.238.1.143
